"use strict";

let slider = tns({
    container: '.courusel__inner',
    controls: false,
    items: 1,
    slideBy: 'page',
    autoplay: true,
    autoplayButtonOutput: false,
    speed: 900,
    navPosition: "bottom",
    // animateIn: "tns-fadeIn"
});